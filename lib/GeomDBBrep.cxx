#include "GeomDB.h"

size_t GeomDB::AddRegion( size_t * ids, int * types, size_t size )
{
  return ERROR_ID;
}

size_t GeomDB::AddSolid( size_t * ids, int * types, size_t size )
{
  return ERROR_ID;
}
